{
  "name": "mkobj",
  "version": "1.0.0",
  "main": "dist/index.js",
  "bin": {
    "mkobj": "./dist/index.js"
  },
  "scripts": {
    "initiate": "npx tsc && node dist/index.js",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sam Voelker",
  "license": "ISC",
  "description": "This CLI creates an object that can be updated and deleted as well as output the store",
  "devDependencies": {
    "@types/node": "^20.14.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "figlet": "^1.7.0"
  }
}
